fastapi>=0.110
python-dotenv>=1.0
uvicorn[standard]>=0.29
pydantic>=2.0
numpy>=1.26
scikit-learn>=1.3          # TF-IDF 폴백용
sentence-transformers>=2.7 # 임베딩 필수 — pipeline/models/ko-sroberta-reco(로컬 파인튜닝) 로드용.
                           # 모델이 없으면 bge-m3 다운로드(~2GB), 그것도 안 되면 TF-IDF 폴백.
                           # torch 를 끌고 오는데, Dockerfile 에서 CPU 전용 휠을 먼저 깔아둔다.
anthropic>=0.40            # 조합 분석(/api/portfolio). 추천엔 불필요
python-dotenv>=1.0         # 프로젝트 루트 .env 자동 로드. 미설치면 export 로 대체 가능
