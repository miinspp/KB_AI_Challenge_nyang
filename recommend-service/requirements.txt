fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
numpy>=1.26
scikit-learn>=1.3          # TF-IDF 폴백용
sentence-transformers>=2.7 # 임베딩 필수 — pipeline/models/ko-sroberta-reco(로컬 파인튜닝) 로드용.
                           # 모델이 없으면 bge-m3 다운로드(~2GB), 그것도 안 되면 TF-IDF 폴백.
anthropic>=0.40            # Haiku 근거 생성 + 에이전트 (ANTHROPIC_API_KEY 필요)
httpx>=0.27                # 에이전트가 진단 API 호출용
