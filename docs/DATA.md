# 데이터 범위 및 출처 정리

> 마지막 갱신: 2026-07-20 (데이터 기준 분기: **2025 Q2 ~ 2026 Q1**)
> 한 줄 요약: **매출 분포는 "서울 한정" 추정 통계, 이익률 벤치마크는 "전국" 조사 통계.** 홈택스 등 개별 점포의 신고·실측 데이터는 포함되지 않는다.

---

## 1. 지역 범위 (가장 중요)

| 산출 요소 | 지역 범위 | 왜 |
|---|---|---|
| 매출 백분위 분포 (상위 % 산출의 핵심) | **서울 한정** | 서울시 상권분석서비스 데이터 (서울 상권 1,576개) |
| 이익률 벤치마크 (순수익·비용효율 계산) | **전국** | 소상공인실태조사는 전국 표본조사 |

⚠️ 따라서 서비스가 말할 수 있는 것은 **"서울 △△업종 (추정매출 기준) 상위 X%"** 이다.
"전국 상위 X%" 라는 표현은 현재 데이터로는 근거가 없으므로 사용 금지.

## 2. 데이터 출처 (2개 공공 소스)

### ① 서울시 상권분석서비스 「추정매출-상권」 — 분포의 본체
- 제공: 서울 열린데이터광장 [OA-15572](https://data.seoul.go.kr/dataList/OA-15572/S/1/datasetView.do) Open API (무료)
- 성격: 서울시가 **카드사 결제 데이터 등 빅데이터로 "추정"** 하여 상권 단위로 집계·공개한 통계
  - 개별 가게 식별 정보 없음. 최소 단위 = **상권 × 업종** (개인정보·과세정보 아님)
  - 점포주가 신고/입력한 값이 아님 → **홈택스와 무관**
- 단위: 분기 × 상권(1,576개: 골목상권·발달상권·전통시장·관광특구) × 업종
- 주요 필드: 분기 추정매출액(`THSMON_SELNG_AMT`), 점포수(`STOR_CO`, 별도 「점포-상권」 API 조인)

### ② 소상공인실태조사 (통계청·중소벤처기업부) — 이익률 벤치마크
- 전국 조사. 업종별: 2022년 확정 / 전체 평균: 2023년 잠정 ([kosis.kr](https://kosis.kr))
- 사용 값: 업종 그룹별 **평균 영업이익률** (`pipeline/benchmarks.json`)

| 그룹 | 평균 영업이익률 |
|---|---|
| 숙박·음식점업 | 22.1% |
| 도·소매업 | 10.6% |
| 교육서비스업 | 35.7% |
| 예술·스포츠·여가업 | 23.1% |
| 수리·기타 서비스업 | 30.2% |
| 소상공인 전체 평균 | 12.6% |

## 3. 현재 적재된 데이터 규모

| 항목 | 값 |
|---|---|
| 기준 분기 | 2025 Q2, Q3, Q4, 2026 Q1 (최신 4개 분기) |
| 상권 수 | 1,576개 (서울 전역) |
| 업종 수 | **60개** (상권 30개 미만 업종 2개 제외) |
| └ 도·소매업 | 29개 |
| └ 숙박·음식점업 | 11개 |
| └ 수리·기타 서비스업 | 7개 |
| └ 예술·스포츠·여가업 | 5개 |
| └ 교육서비스업 | 4개 |
| └ 기타(전체 평균 적용) | 4개 |
| 반영 점포 수 (가중치 합) | 약 306,625개 |
| 원본 관측치 | 매출 85,493행 / 점포 304,364행 (최신 4분기) |
| 상권유형별 보조 분포 | 골목상권 53개 · 발달상권 55개 · 전통시장 36개 업종 (관광특구는 전 업종 표본 미달로 제외) |

## 4. 가공 흐름

```
서울 Open API (JSON 응답, ~206만 건 전량 수집)
  → pipeline/raw/*.csv               (원본 보관, git 미추적)
  → 점포당 월매출 = 분기 추정매출 ÷ 3 ÷ 점포수
  → 상권×업종 관측치를 점포수로 가중한 업종별 백분위(0~100) 격자
     (+ 상권유형[골목/발달/전통시장]별 보조 격자 — 표본 상권 30개 이상 유형만)
  → backend/src/main/resources/data/industry_distributions.json  (DB 없음, 기동 시 메모리 로드)
```

재수집·갱신 명령은 `pipeline/collect_seoul_sales.py` 상단 주석과 README 참고.

## 4-1. 비용구조·임대료 벤치마크 (산출식 v2 보정축 근거)

> 산출물: `backend/src/main/resources/data/public_benchmarks.json`
> 수집 스크립트: `pipeline/collect_public_benchmarks.py`
> 용도: 비용 구조 축을 "10% 경험칙"에서 **업종별 실측 평균 대비**로 승격 + 리포트 비교 표기.

| 데이터 | 출처 / 표ID | 기준 기간 | 수집 방식 | 서비스 사용처 |
|---|---|---|---|---|
| 업종별 영업비용 비목 (매출원가·급여·임차료·기타) | 소상공인실태조사 `DT_3ME0144` (중기부·KOSIS) | **2023년** (기업체당 연간) | 스냅샷¹ | 원가율·인건비율·**임차료율** 업종 벤치마크 → 비용구조 점수 |
| 업종별 기업체당 보증금·월세 | 소상공인실태조사 `DT_3ME0132` | **2024년** | 스냅샷¹ | 평균 월세 참고 표기 |
| 업종별 매출액·영업이익 | 중기부 보도자료(2023 잠정, 2025-02 발표) | **2023년** | 스냅샷 | 이익률·비목 비율 분모(매출), 항등식 검증 |
| 서울 상가 임대료 (소규모·중대형·집합) | 한국부동산원 임대동향조사 R-ONE `T248223…`/`T244363…`/`T244913…` | **2024 Q3 ~ 최신** (분기) | **라이브 API**² | 서울 상권별 ㎡당 임대료 컨텍스트 |

¹ **스냅샷 사유**: KOSIS 뷰어의 내부 조회 POST(`html.do`)가 세션 토큰에 의존해 무인증 재현이 불안정.
  연 1회 갱신되는 소량(업종 대분류 12행) 통계라, 뷰어에서 실제 조회한 값을 표ID·기준연도·추출일(스크립트 `KOSIS_SNAPSHOT_DATE`)과 함께 상수로 고정하고,
  **매출 − 영업이익 == 영업비용** 항등식으로 두 출처(KOSIS·보도자료) 정합성을 자동 검증한다(불일치 시 수집 실패).
² **라이브 사유**: R-ONE 뷰어의 JSON 엔드포인트(`sttsDataPreviewList.do`)는 무인증·무세션으로 응답 → 실행 시점 최신 분기를 그대로 수집.

### 업종 그룹별 벤치마크 (2023, 연간 매출 대비 비율)

| 그룹 | 영업이익률 | 원가율 | 인건비율 | 임차료율 | 평균 월세 |
|---|---|---|---|---|---|
| 숙박·음식점업 (food) | 21.2% | 46.4% | 15.9% | 9.3% | 102만원 |
| 도·소매업 (retail) | 9.2% | 65.4% | 11.2% | 6.2% | 99만원 |
| 교육서비스업 (education) | 32.0% | 20.0% | 25.3% | 14.7% | 92만원 |
| 예술·스포츠·여가업 (leisure) | 25.0% | 29.3% | 18.5% | 15.2% | 144만원 |
| 수리·기타 서비스업 (repair_personal) | 29.8% | 40.3% | 11.9% | 10.4% | 74만원 |
| 소상공인 전체 (overall) | 12.6% | 56.8% | 15.1% | 7.0% | 101만원 |

> 비용구조 점수 = 사용자의 임차료율·인건비율·원가율(입력분)을 각 업종 평균과 비교(평균이면 50점, 절반이면 75점, 2배면 0점)해 평균.
> `public_benchmarks.json` 이 없으면 백엔드는 자동으로 기존 "임대료 10% 경험칙"으로 폴백한다(하위호환).

### 상권유형별 임차료율 지역 보정 (한국부동산원 서울 상가 임대료)

임차료율 벤치마크(전국 평균)는 서울 상권의 임대료 수준을 반영하지 못한다. 그래서
한국부동산원 R-ONE **서울 상가유형별 ㎡ 임대료**를 상권유형에 대응시켜 **완만한 보정 배수**를 만든다.

| 상권유형 (서울시 상권분석) | ↔ R-ONE 상가유형 | 서울 임대료(천원/㎡)* | 보정 배수 |
|---|---|---|---|
| 골목상권 (가로변 소형) | 소규모상가 | 52.5 | **×1.00** (기준) |
| 발달상권 (번화가 중대형) | 중대형상가 | 56.4 | **×1.074** |
| 전통시장 (시장 내 구분) | 집합상가 | 49.6 | **×0.945** |

\* 최신 분기(2026 Q1) 서울 평균. 배수 = 각 상가유형 ÷ 소규모상가.

- **적용**: `조정 임차료율 벤치마크 = 전국 임차료율 × 보정배수`. 상권유형을 고른 경우에만 적용하며,
  "서울 전체"(미선택)에는 전국 벤치마크를 그대로 쓴다.
- **감쇠 없음 근거**: ㎡ 임대료와 임차료율(임차료÷매출)은 1:1 비례하지 않는다(번화가는 매출도 높음).
  상가유형 간 실측 격차 자체가 이미 ±10% 내로 완만하므로 임의 감쇠 계수 없이 그대로 쓴다.
- **효과 예**: 임대료 부담률 12%인 한식집 → 발달상권 39.8점(관대) · 골목 35.3점 · 전통시장 31.5점(엄격).
- **한계**: R-ONE 지역 구분(도심/강남/…)과 상권유형(골목/발달/전통)은 taxonomy가 달라, 상가유형(소규모/중대형/집합)을
  프록시로 삼은 근사 대응이다(스크립트 `AREATYPE_SHOPTYPE`). 매출 분포는 서울, 비용 벤치마크는 전국 기준이라는 지역 혼재는 유지된다.

> 산출식 전체 흐름·가중치는 [`METHODOLOGY.md`](METHODOLOGY.md) 참고.

## 4-2. AI 추천 보강 데이터 (정책·KB상품 키워드/임베딩)

정책 공고(`seoul_policies_sosangongin.json` 37건)와 KB상품(`kb-products/recommendable_products.json` 24건)은
긴 프로즈(`summary`, `public_conditions_summary`) 위주라 그대로는 매칭·노출이 어렵다.
오프라인 배치로 두 데이터를 공통 스키마로 정규화하고 AI 보강을 붙인다.

- **배치**: `pipeline/enrich_recommendables.py`
  - 키워드 추출: KeyBERT + kiwipiepy 명사 후보(단일어·연속 2-gram), 공고문 일반어 불용어 제거,
    금융 용어(대출·보증·이차보전 등) 화이트리스트 가산(+0.15) → 항목당 상위 5개
  - 임베딩: `ko-sroberta-reco`(B단계 파인튜닝, 없으면 베이스 `jhgan/ko-sroberta-multitask`),
    768차원 정규화 — 제목+요약+카테고리+지원유형 텍스트. 키워드용 백본은 베이스 모델로 분리.
  - 산출물: `backend/src/main/resources/data/reco/enriched_items.json` (61건, 키워드+임베딩+앵커 포함)
- **매칭 프로토타입**: `pipeline/recommend_match.py`
  - 진단 신호(상위 %·임대료 부담률·매출 추세·업력)를 자연어 프로필 문장으로 변환 → 임베딩 코사인 유사도 랭킹
  - 하드 필터: 마감일 경과·`max_biz_age` 초과·지역 불일치 제외
  - 규칙 가드: 폐업·재기류 항목은 심각한 위기 신호(하위 25% + 하락) 또는 명시 언급 없으면 감점(−0.10)
  - 샘플 프로필 `struggling`/`growth`/`startup`으로 결과 분화 검증 완료
- **3줄 요약 (자체 파인튜닝 모델)**: `pipeline/train_summarizer.py` + `pipeline/summarize_items.py`
  - 태스크: 공고 원문 → "지원:/대상:/신청:" 구조화 3줄 요약 (금융 핵심정보 중심)
  - 학습 데이터: `pipeline/data/policy_summaries.jsonl` — 전체 공고 439건 중 200건에 대해
    Claude가 teacher로 gold 요약을 작성한 지식 증류(distillation) 데이터셋.
    서비스 노출분(소상공인 37건)은 학습에서 제외하고 검증셋으로 사용 → unseen 일반화 측정
  - 베이스 모델: `EbanLee/kobart-summary-v3` (KoBART 요약 체크포인트) → 163건으로 6 epoch
    도메인·형식 적응 (MPS 로컬 학습, best val_loss 0.918 @ epoch 3)
  - 검증 성능(unseen 37건): 형식 준수 0%→92%, 형태소 ROUGE-1 F1 0.235→0.727,
    gold 대비 임베딩 유사도 0.752→0.942 (zero-shot 대비)
  - 적용: 생성 결과 후처리(중복 토큰 정리) 후 형식 검증 실패 시 gold 폴백(37건 중 3건)
    → `enriched_items.json`의 `summary_short` 필드
  - 모델 가중치는 `pipeline/models/`(git 미추적), `train_summarizer.py`로 재생성 가능
- **추천 임베딩 (contrastive 파인튜닝)**: `pipeline/train_matcher.py` + `pipeline/eval_matcher.py`
  - 문제: 진단 프로필과 정책·상품을 매칭하는 학습 데이터가 없음.
  - 학습 데이터: 업종×위치×임대료×추세×업력×관심사를 조합한 **사장님 프로필 60개**를 생성하고,
    Claude teacher가 각 프로필에 적합한 항목을 라벨링(`pipeline/data/reco_pairs.jsonl`, 442 양성 쌍).
    프로필 id 기준 8:2 분할 → 검증 12개(`reco_val_profiles.json`)는 학습에서 제외.
  - 방식: 베이스 `jhgan/ko-sroberta-multitask` → `MultipleNegativesRankingLoss`(배치 내 자동 음성)로
    4 epoch 학습 (MPS, 약 3분). 소량 데이터에 강한 in-batch negative 대비 학습.
  - 검증 성능(unseen 프로필 12개, 정답=teacher 라벨): **Recall@5 0.133→0.467, MRR 0.252→0.667,
    NDCG@5 0.127→0.460** (베이스 대비 3배 이상 개선)
  - 반영: 파인튜닝 모델로 항목·앵커 임베딩을 재생성 → 백엔드(앵커 합성)와 동일 공간이라 코드 변경 없이 호환.
- **백엔드 서빙 (`POST /api/recommend`)**: `com.nyang.reco` — 배치가 사전계산한 **니즈 앵커
  임베딩 8종**(하위권·상위권·임대료 부담·매출 하락·창업 초기·자금 조달·판로 확대 등)을
  진단 신호에 따라 가중 합성해 프로필 벡터를 만들고 코사인 매칭. 하드 필터(마감·지역·업력)와
  폐업류 감점 규칙 포함. **런타임 모델 추론이 전혀 없어** Java 단독으로 동작(내적 61회).
- **프론트 노출**: 추천 화면(`RecommendScreen`)에 "🤖 AI 맞춤 추천" 섹션 — 진단 신호 배지 +
  정책 카드 5·KB상품 카드 3, 각 카드에 3줄 요약·키워드 칩·매칭%·추천 사유.
- **런타임 비용**: 무거운 연산(키워드·문서 임베딩·3줄 요약 생성·앵커 인코딩)은 전부 배치에서 사전계산.
- **한계**: 자격요건이 프로즈에 묻혀 있어 하드 필터가 불완전(업력·지역만 구조화됨). 유사도 혼동
  (위기지원 vs 폐업지원)은 규칙 가드로 보완. 파인튜닝 라벨은 Claude teacher 판단이라 실제 심사기준과
  다를 수 있음(합성 데이터의 근본 한계). 향후 KB-ALBERT(금융특화 인코더) 수령 시 베이스 교체 예정.

### 파이프라인 실행 순서 (추천 기능 재생성)

```
.venv/bin/python pipeline/train_summarizer.py   # 1. KoBART 3줄 요약 모델 학습 (선택)
.venv/bin/python pipeline/train_matcher.py       # 2. 추천 임베딩 contrastive 학습 (선택)
.venv/bin/python pipeline/enrich_recommendables.py  # 3. 키워드·임베딩·앵커 생성
.venv/bin/python pipeline/summarize_items.py     # 4. 3줄 요약 필드 채우기
# → backend/src/main/resources/data/reco/enriched_items.json 갱신, 백엔드 재시작 시 반영
```
학습(1·2)은 모델 가중치가 `pipeline/models/`에 있으면 건너뛸 수 있고, 없으면 3단계가 베이스 모델로 폴백한다.

## 5. 데이터가 "말할 수 없는 것" (한계)

1. **전국 순위** — 분포가 서울 한정. 전국 확장 시 상권정보시스템(소진공) 등 별도 소스 필요.
2. **실측 순수익 분포** — 타 점포의 순이익·원가율은 세법상 보호 대상이라 어떤 경로로도 수집 불가.
   현재는 매출 분포 × 업종 평균 이익률로 **유도한 근사 분포**를 사용 (점포별 이익률 편차 미반영, API 응답 notes에 명시).
3. **개별 점포 정확 매출** — 원천이 카드 데이터 기반 "추정치"이며 상권 단위 집계임.
4. 본인 데이터(매출·지출)는 사용자 직접 입력에 의존. 향후 홈택스 본인 동의 연동(삼쩜삼 방식)으로 대체 가능.
